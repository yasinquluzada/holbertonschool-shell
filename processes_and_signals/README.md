# processes_and_signals

Project about processes and signals in Bash.  
0)
Contains scripts like `0-what-is-my-pid` to display its own PID.
1)
Display a list of all currently running processes in a user-oriented hierarchical view
